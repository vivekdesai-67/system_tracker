<!-- Splash Screen Overlay -->
<div id="splash-screen" style="
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: var(--bg-primary);
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
">
    <div class="splash-content" style="
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: var(--space-4);
    ">
        <!-- Replace with an actual SVG logo for better quality -->
        <div style="
            width: 80px; 
            height: 80px; 
            background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary)); 
            border-radius: var(--radius-xl);
            box-shadow: var(--shadow-glow);
        "></div>
        <h1 style="font-size: var(--text-3xl); letter-spacing: -1px;">ProjectTrack</h1>
    </div>
</div>
